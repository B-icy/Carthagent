# Optional path to Carthagent / Pi CLI for extension integration tests
CARTHAGENT_CLI=
PI_CLI=

# Optional Carthagent Cloud account/control-plane override (preview or staging)
CARTHAGENT_CLOUD_URL=

# Optional direct upstream gateway override. Do not commit managed gateway keys.
EXP_GATEWAY_URL=

# Optional Inception Platform API key for live flow tests
INCEPTION_API_KEY=
